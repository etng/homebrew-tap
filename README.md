# homebrew-tap

Homebrew tap for etng tools.

## Usage

```bash
brew tap etng/tap
brew install codex-bro
```

## Notes

- Formula files live in `Formula/`.
- Release automation such as GoReleaser can update this repository directly.
